
CREATE  TABLE IF NOT EXISTS `raspberrypicodebased_DEVICE` (
  `raspberrypicodebased_DEVICE_ID` VARCHAR(45) NOT NULL ,
  `DEVICE_NAME` VARCHAR(100) NULL DEFAULT NULL,
  PRIMARY KEY (`raspberrypicodebased_DEVICE_ID`) )
ENGINE = InnoDB;




